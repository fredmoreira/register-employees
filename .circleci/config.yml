version: 2
jobs:
  build:
    working_directory: ~/register-employees
    docker:
      - image: circleci/node:latest
      - image: mongo:4.0.3
    steps:
      - checkout
      - run:
          name: update-npm
          command: 'sudo npm install -g npm@5'
      - restore_cache:
          key: dependency-cache-{{ checksum "package-lock.json" }}
      - run:
          name: install-npm-register-employees
          command: npm install
      - save_cache:
          key: dependency-cache-{{ checksum "package-lock.json" }}
          paths:
            - ./node_modules
      - run:
          name: lint-code
          command: npm run code:format
